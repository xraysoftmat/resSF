
repos:
# Pre-commit hooks for basic Python processing
-   repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
    -   id: check-added-large-files
        args: ['--maxkb=1500']  # 1.5MB limit for added files
    -   id: check-toml
    -   id: check-yaml
    -   id: end-of-file-fixer
    -   id: mixed-line-ending
    -   id: trailing-whitespace

# # Black - for standard code formatting
# -   repo: https://github.com/psf/black
#     rev: 24.10.0
#     hooks:
#     -   id: black

# Ruff - for code linting and style checking
- repo: https://github.com/astral-sh/ruff-pre-commit
  # Ruff version.
  rev: v0.14.5
  hooks:
    # Run the linter.
    - id: ruff-check
      args: [ --fix]
    # Run the formatter.
    - id: ruff-format

# NumPyDoc - for docstring validation
- repo: https://github.com/numpy/numpydoc
  rev: v1.10.0
  hooks:
    - id: numpydoc-validation

- repo: https://github.com/commitizen-tools/commitizen
  rev: v4.1.0  # Use the latest stable version
  hooks:
    - id: commitizen
      stages: [commit-msg]
    - id: commitizen-branch
      stages: [pre-push]